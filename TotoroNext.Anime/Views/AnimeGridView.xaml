<UserControl
    x:Class="TotoroNext.Anime.Views.AnimeGridView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TotoroNext.Anime.Views"
    xmlns:m="using:TotoroNext.Anime.Abstractions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:TotoroNext.Anime.UserControls"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <ItemsView ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
        <ItemsView.ItemTemplate>
            <DataTemplate x:DataType="m:AnimeModel">
                <ItemContainer>
                    <uc:AnimeCard Anime="{x:Bind}" />
                </ItemContainer>
            </DataTemplate>
        </ItemsView.ItemTemplate>
        <ItemsView.Layout>
            <UniformGridLayout
                ItemsJustification="Start"
                ItemsStretch="Fill"
                MaximumRowsOrColumns="3"
                MinColumnSpacing="16"
                MinItemHeight="300"
                MinItemWidth="125"
                MinRowSpacing="16" />
        </ItemsView.Layout>
    </ItemsView>
</UserControl>
